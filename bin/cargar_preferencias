#!/bin/sh


# Estas son los programas y directorios predeterminados.
export PATH="$PATH:$(du "$HOME/.local/bin" | cut -f2 | paste -sd ':' -)"
export SUDO_ASKPASS="$HOME/.local/bin/menu_pass"
export BROWSER=firefox
export READER=gedit
export TERMINAL=kitty
export EDITOR=gedit
